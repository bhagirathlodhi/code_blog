<nav class="navbar navbar-expand-lg navbar navbar-dark bg-dark">
  <div class="container">
  
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if user_signed_in?   %>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="<%= user_posts_path %>">my blogs</i></a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="<%= root_path%>">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="<%= new_post_path %>">new blog</a>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link active dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="bi bi-person"></i><%= current_user.full_name %>
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
              <li><%= button_to "Log Out", destroy_user_session_path, class: "dropdown-item", method: :delete %></li>
              <li><a class="dropdown-item" href="<%= edit_user_registration_path %>">Update profile</a></li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item">
            <a class="nav-link active" href="<%= new_user_session_path %>">Login</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="<%= new_user_registration_path %>">Signup</a>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>